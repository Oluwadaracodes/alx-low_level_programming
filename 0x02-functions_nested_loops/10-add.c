#include "main.h"

/**
 * add - 'adds two integers together and returns the result'
 *
 * @a: one of the integers to be added
 * @b: two of the integers to be added
 * Return:integer result of addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
